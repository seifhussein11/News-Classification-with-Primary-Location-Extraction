Project in collaboration with Spearfish Security LTD. A whole pipeline was created that included the classification of news articles using fine-tuned and prompt engineered models such as Llama and DistilBERT. 
After Classification, the primary location of the main event happening in the article is extracted using fine-tuned and prompt-engineered models.
